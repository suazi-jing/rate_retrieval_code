# 💳 礼品卡回收智能客服系统

> 为公司礼品卡回收业务搭建的全链路 AI 智能客服系统，覆盖数据采集、智能计算、消息触达三大环节，实现从客户咨询到自动报价的秒级响应。

---

## 📊 项目成果

| 指标 | 优化前 | 优化后 |
|------|--------|--------|
| 日均处理咨询量 | - | **30,000 条** |
| 报价响应时间 | 10 分钟（人工） | **13 秒（自动）** |
| 支持卡种 | - | **12 种** |
| 系统可用性 | - | 7×24 小时 |

---

## 🏗️ 系统架构
┌─────────────────────────────────────────────────────────┐
│ 礼品卡智能客服系统 │
├──────────────┬──────────────────┬───────────────────────┤
│ 数据采集模块 │ 智能计算模块 │ 消息触达模块 │
│ │ │ │
│ Xposed Hook │ Dify 工作流引擎 │ WhatsApp Webhook │
│ 汇率实时抓取 │ 意图识别+知识库 │ wasender API 联调 │
│ 数据清洗入库 │ LLM 生成+SQL查询 │ 多轮对话记忆 │
└──────────────┴──────────────────┴───────────────────────┘

---

## 🚀 功能模块

### 1. 数据采集模块

针对封闭 IM 平台的汇率数据，采用 **Android 模拟器 + Xposed Hook** 非侵入式采集方案：

- 通过运行时 Hook 框架拦截目标应用消息事件
- 实现汇率广告的实时捕获与文本化
- 数据自动清洗、结构化入库 MySQL

IM平台消息 → Xposed Hook 拦截 → 数据清洗 → MySQL 入库


### 2. 数据拆分入库模块

原始汇率广告包含大量行业术语，需要结构化处理后才能使用：

- **lx_db_insert.py**：从 MySQL 抽取原始汇率广告，远程调用 Dify Workflow API
- **Dify 工作流**：LLM 节点将广告拆分为 XML 格式的结构化数据
- **bd_insert_api.py**：接收 Dify HTTP 请求，将结构化数据入库，并删除已处理的原始记录
原始广告数据 → Python 抽取 → Dify LLM 拆分 → XML 结构化 → API 入库 → 删除原始数据


### 3. 智能报价模块（Dify 工作流）

**webhook_server.py** 部署在服务器上，调用 wasender API 监听 WhatsApp 消息，捕获后回调 Dify 工作流：

#### 工作流程
用户咨询 → Webhook 监听 → Dify 工作流
│
├── ① 意图分类（问价/闲聊）
├── ② 知识库检索（12种卡类规则 + 外汇值）
├── ③ LLM 生成 JSON（卡名、国家、卡类型）
├── ④ 代码执行组合查询 → 生成 SQL
├── ⑤ 数据库查询 + 外汇值 + 卡标签
└── ⑥ 自动计算报价 → wasender API → 回复用户


#### 核心能力

- **意图识别**：自动区分问价信息和闲聊信息，分流处理
- **知识库检索**：内置 12 种礼品卡询问规则和实时外汇值查询
- **LLM 结构化生成**：根据知识库规则和用户问题，生成 JSON 格式查询参数
- **动态 SQL 查询**：代码节点组合卡标签（卡名 + 国家 + 卡类型），拼接 SQL 语句查询数据库
- **自动报价计算**：综合 LLM JSON 数据、知识库外汇值、数据库查询结果，自动计算最终报价
- **多轮对话记忆**：通过 API 调用 Dify 实现会话记忆功能，支持上下文连续对话

---

## 🛠️ 技术栈

| 类别 | 技术 |
|------|------|
| 后端语言 | Python |
| AI 引擎 | Dify 工作流引擎 |
| 数据库 | MySQL |
| 消息服务 | wasender API（WhatsApp） |
| 数据采集 | Xposed Hook（Android） |
| Web 服务 | Flask（Webhook） |

---

## 📁 项目结构
rate_retrieval_code/
├── webhook_server.py # WhatsApp Webhook 监听服务
├── lx_db_insert.py # 汇率数据抽取与 Dify 调用
├── bd_api.py # 结构化数据入库 API
├── db_delete_api.py # 数据清理接口
├── cuangjbiao.sql # MySQL 建表 SQL 脚本
│
├── 客服机器人初始.yml # Dify 工作流：客服机器人初始化配置
├── 停群删除汇率数据.yml # Dify 工作流：停止群组监听与汇率数据清理
├── 广告导入加数据库导入.yml # Dify 工作流：汇率广告导入与数据库入库
├── start.sh # 服务启动脚本
└── stop.sh # 服务停止脚本


---
## 🔧 快速开始

### 环境要求

- Python 3.8+
- MySQL 5.7+
- Dify 平台账号
- WasenderAPI

### 部署步骤

```bash
git clone https://github.com/suazi-jing/rate_retrieval_code.git
cd rate_retrieval_code


📝 许可证
本项目为内部项目，仅供公司业务使用。

👤 作者
suazi-jing - GitHub

⭐ 如果这个项目对你有帮助，请给个 Star！